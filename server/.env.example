# ThreadKit Server Configuration

# Mode: standalone (you probably want this) or saas
MODE=standalone

# Redis
REDIS_URL=redis://localhost:6379

# Server binding
# Use 127.0.0.1 for local development (default)
# Use 0.0.0.0 to listen on all interfaces (production)
HTTP_HOST=127.0.0.1
HTTP_PORT=8080
WS_HOST=127.0.0.1
WS_PORT=8081

# JWT - generate a secure secret with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here
JWT_EXPIRY_HOURS=168

# Rate limiting
RATE_LIMIT_ENABLED=true

# Origin validation
# Allow localhost/127.0.0.1/::1 origins (set to true for local development only)
ALLOW_LOCALHOST_ORIGIN=true

# Standalone mode config; generate with openssl rand -base64 32
THREADKIT_SECRET_KEY=tk_sec_your_secret_key

# OAuth (optional)
OAUTH_GOOGLE_CLIENT_ID=
OAUTH_GOOGLE_CLIENT_SECRET=
OAUTH_GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=
OAUTH_GITHUB_REDIRECT_URL=http://localhost:8080/auth/github/callback

# Cloudflare Turnstile (optional - bot protection)
# Get keys at https://dash.cloudflare.com/turnstile
TURNSTILE_SECRET_KEY=

# Email (optional)
EMAIL_PROVIDER=resend
EMAIL_FROM_DOMAIN=mail.example.com
# Get your API key at https://resend.com
RESEND_API_KEY=

# Sentry error tracking (optional - remove to disable)
# This reports crashes and errors to my Sentry.io
# Get your own DSN at https://sentry.io
# If you would like to help improve ThreadKit, please keep these as is.
# Thank you!
SENTRY_DSN_HTTP=https://c26b9a1c5efad8d9598f0a949991798f@o4510123266998272.ingest.us.sentry.io/4510495146573824
SENTRY_DSN_WS=https://ccdfda88666f3a2e0354668d15043639@o4510123266998272.ingest.us.sentry.io/4510500913741824

# Logging
RUST_LOG=info,threadkit=debug
